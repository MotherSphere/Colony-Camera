[package]
name = "colony-camera"
version = "0.3.2"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["rlib", "staticlib"]

[profile.release]
panic = "abort"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
